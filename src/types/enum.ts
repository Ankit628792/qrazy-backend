export enum ORDER_STATUS {
    "PENDING" = "PENDING",
    "RECEIVED" = "RECEIVED"
}

export enum QR_Type {
    "DIGITAL" = "DIGITAL",
    "PHYSICAL" = "PHYSICAL"
}

export enum QR_STATUS {
    "ACTIVE" = "ACTIVE",
    "EXPIRED" = "EXPIRED",
    "USED" = "USED"
}